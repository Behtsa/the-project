const BASE_ENDPOINT = 'https://rickandmortyapi.com/api';

export const API_ENDPOINTS = {
    CHARACTER: `${BASE_ENDPOINT}/character`,
    LOCATIONS: `${BASE_ENDPOINT}/location`,
    EPISODES: `${BASE_ENDPOINT}/episodes`,
}
